/**
 * Playwright Global Teardown
 * Cleanup tasks setelah semua Playwright tests
 */

async function globalTeardown(config) {
  // Cleanup test data yang mungkin tersisa
  // Add cleanup logic jika needed
  
  }

module.exports = globalTeardown;
